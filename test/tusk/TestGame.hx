package tusk;

import buddy.*;
import tusk.events.*;
using buddy.Should;

class TestGame extends BuddySuite {
	public function new() {
		describe('Using a game', {
			it('should be able to subscribe to core events');
		});
	}
}